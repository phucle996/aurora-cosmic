[project]
name = "aurora-ml-worker"
version = "0.1.0"
description = "AURORA Python ML Worker for Exoplanet Candidate & Anomaly Detection Training"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
