[project]
name = "aurora-dashboard"
version = "0.1.0"
description = "AURORA Scientific Dashboard for Light Curve, TPF & Anomaly Visualization"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
