[Unit]
Description=AURORA ClickHouse OLAP Database (native)
PartOf=aurora.target

[Service]
Type=simple
ExecStart=%h/.local/bin/clickhouse server --config-file=%h/.config/aurora/clickhouse/config.xml
Restart=on-failure
RestartSec=3
KillSignal=SIGINT

[Install]
WantedBy=aurora.target
